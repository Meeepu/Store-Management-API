import { RequestHandler } from "express";

export const getStores: RequestHandler = (req, res, ) => {
    
}

export const createStore: RequestHandler = (req, res, ) => {
    
}

export const updateStore: RequestHandler = (req, res, ) => {
    
}

export const deleteStore: RequestHandler = (req, res, ) => {
    
}